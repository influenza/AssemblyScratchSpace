# Apparently the default is 32-bit mode
set $64BITS = 1
# I write in intel style, so I want to read it too
set disassembly-flavor intel
# This project benefits from watching the registers
layout reg

# Should fix layout issues
set height 0
set width 0
